<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data>

    <!-- Wizard Views -->
    <record id="view_account_analytic_propagate_create_form" model="ir.ui.view">
      <field name="name">account.analytic.propagate.create.form</field>
      <field name="model">account.analytic.propagate.create</field>
      <field name="arch" type="xml">
        <form string="Create Analytic Accounts">
          <group name="create_form">
            <field name="group_id" />
          </group>
          <group name="create_form_accounts">
            <notebook>
              <page name="create_values" string="Create Values">
                <div>
                  <button name="button_unlink_wizard_line" type="object"
                    attrs="{'invisible':[('wizard_line', '=', [])]}"
                    icon="fa-times" string="Clear Values"
                    confirm="This will clear all current lines. Are you sure you want to continue?" />
                </div>
                <field name="wizard_line">
                  <tree editable="bottom" >
                    <field name="parent_id" />
                    <field name="name" />
                    <field name="code" />
                  </tree>
                </field>
              </page>
            </notebook>
          </group>
          <footer>
            <button string="Create" name="button_create" type="object" class="btn-primary" confirm="This will create the analytic accounts. Do you want to continue?" />
            <button string="Cancel" class="btn-default" special="cancel" />
          </footer>
        </form>
      </field>
    </record>

    <!-- Actions -->
    <record id="action_account_analytic_propagate_create" model="ir.actions.act_window">
      <field name="name">Create Analytic Accounts</field>
      <field name="res_model">account.analytic.propagate.create</field>
      <field name="view_type">form</field>
      <field name="view_mode">tree,form</field>
      <field name="view_id" ref="view_account_analytic_propagate_create_form" />
      <field name="target">new</field>
    </record>

    <!-- Menus -->

  </data>
</odoo>