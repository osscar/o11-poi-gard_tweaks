<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Search Views -->

        <!-- Tree Views -->

        <!-- Form Views -->
        <record id="view_ir_exports_form_inherit" model="ir.ui.view">
            <field name="name">ir.exports.form.inherit</field>
            <field name="model">ir.exports</field>
            <field name="inherit_id" ref="base.export_config_form_view"/>
            <field name="arch" type="xml">
                <form string="Export Configuration">
                    <field name="name"/>
                    <field name="model_id"/>
                    <!-- Include the is_import_friendly field in the form view -->
                    <field name="is_import_friendly"/>
                    <!-- Other fields as needed -->
                    <footer>
                        <button name="save" string="Save" type="object" class="btn-primary"/>
                        <button name="cancel" string="Cancel" type="object" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>
        

        <!-- Kanban Views -->

        <!-- Actions -->

        <!-- Menus -->

    </data>
</odoo>