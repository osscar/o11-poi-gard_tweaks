<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <record id="group_export_list_users" model="res.groups">
            <field name="name">Export List Users</field>
            <!-- Define access rules if needed -->
        </record>

        <record id="group_export_list_users" model="res.groups">
            <field name="name">Export List Users</field>
        </record>
        
        <record id="group_export_list_managers" model="res.groups">
            <field name="name">Export List Managers</field>
        </record>
        
        
        <!-- User Rules -->
        <!-- <record id="access_rule_export_list_users" model="ir.rule">
            <field name="name">Export List Users Access Rule</field>
            <field name="model_id" ref="model_base_import_export"/>
            <field name="domain_force">[('security_group_id', 'in', [user.id for user in [env.ref('your_module.group_export_list_users').users]])]</field>
            <field name="perm_read" eval="True"/>
            <!-- Define other permissions as needed -->
        </record>
        
        <record id="access_rule_export_list_managers" model="ir.rule">
            <field name="name">Export List Managers Access Rule</field>
            <field name="model_id" ref="model_base_import_export"/>
            <field name="domain_force">[('security_group_id', 'in', [user.id for user in [env.ref('your_module.group_export_list_managers').users]])]</field>
            <field name="perm_read" eval="True"/>
            <!-- Define other permissions as needed -->
        </record> -->
        

    </data>
</odoo>