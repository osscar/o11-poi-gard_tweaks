<?xml version="1.0"?>
<openerp>
    <data>

        <record id="invoice_tree" model="ir.ui.view">
            <field name="name">invoice.tree.inherit.gard_order_history</field>
            <field name="model">account.invoice</field>
            <field name="mode">extension</field>
            <field name="inherit_id" ref="account.invoice_tree"/>
            <field name="arch" type="xml">

                <xpath expr="//tree[1]" position="attributes">
                    <attribute name="colors">red:state in 'open' and (residual &gt; 0) and date_due &lt; current_date;blue:state == 'draft';black:state in ('proforma','proforma2','open');gray:state == 'cancel';green: state == 'paid'</attribute>
                </xpath>

            </field>
        </record>

        <record id="invoice_view_tree" model="ir.ui.view">
            <field name="name">invoice.tree.gard_order_history</field>
            <field name="model">account.invoice</field>
            <field name="view_mode">tree</field>
            <field name="type">tree</field>
            <field name="arch" type="xml">

                <tree colors="red:state in 'open' and (residual &gt; 0) and date_due &lt; current_date;blue:state == 'draft';black:state in ('proforma','proforma2','open');gray:state == 'cancel';green: state == 'paid'" fonts="bold: state in 'open' and (residual &gt; 0) and date_due &lt; current_date" string="Invoices">
                    <field name="state"/>
                    <field name="date_due"/>
                    <field name="date_invoice"/>
                    <field name="invoice_number"/>
                    <field name="origin"/>
                    <field name="partner_id"/>
                    <field name="currency_id"/>
                    <field name="residual"/>
                    <field name="amount_total"/>
                </tree>

            </field>
        </record>

    </data>
</openerp>
