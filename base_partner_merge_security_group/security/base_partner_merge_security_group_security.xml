<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="group_partner_merge_manager" model="res.groups">
            <field name="name">Partner Merge Manager</field>
            <field name="category_id" ref="base.module_category_usability"/>
            <field name="comment">Allow user to merge contacts.</field>
            <!-- <field name="implied_ids" eval="[(4, ref('base.group_no_one')),(4, ref('product.group_product_pricelist')),(4, ref('product.group_sale_pricelist'))]"/> -->
            <field name="users" eval="[(4, ref('base.user_root'))]"/>
        </record>

    </data>
</odoo>
