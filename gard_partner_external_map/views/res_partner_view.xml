<?xml version="1.0" encoding="utf-8"?>
<!--
  © 2015-2016 Akretion (Alexis de Lattre <alexis.delattre@akretion.com>)
  License AGPL-3.0 or later (http://www.gnu.org/licenses/agpl).
-->

<odoo>

    <!-- Action confirm_payments for multi -->
    <record id="action_open_route_map" model="ir.actions.server">
        <field name="name">Route Map</field>
        <field name="type">ir.actions.server</field>
        <field name="state">code</field>
        <field name="model_id" ref="base.model_res_partner"/>
        <field name="binding_model_id" ref="base.model_res_partner"/>
        <field name="code">
if len(records) &gt; 1:
    action = records._open_route_map()
else:
    raise Warning('Two or more selected items are required to show a route.')
        </field>
    </record>

</odoo>
